module.exports = function generateButton() {
  const contentDescriptor = {
    description: null,
    props: {
      //className: 'hidden-input'
    },
    parentProps: {
      className: 'hidden-input'
    }
  };

  return contentDescriptor;
};
